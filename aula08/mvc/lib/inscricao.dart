import 'package:flutter/material.dart';

class InscricaoView extends StatelessWidget {
  const InscricaoView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("INSCRIÇÕES"),
    );
  }
}