import 'package:flutter/material.dart';

class AltaView extends StatelessWidget {
  const AltaView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("EM ALTA"),
    );
  }
}