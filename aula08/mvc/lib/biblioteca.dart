import 'package:flutter/material.dart';

class BibliotecaView extends StatelessWidget {
  const BibliotecaView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("BIBLIOTECA"),
    );
  }
}